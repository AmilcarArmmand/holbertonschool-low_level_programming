#include "holberton.h"

/**
 * add - a function that adds two integers and returns the result
 * @a: first operand
 * @b: second operand
 * Return: sum of the two integers
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;

	return (sum);
}
