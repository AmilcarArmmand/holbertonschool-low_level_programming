#include "holberton.h"

/**
 * is_palindrome - a function that returns 1 if string is palindrome, 0 if not.
 * @s: pointer to a string
 *
 * Return: 1 (Success) or 0 (if not)
 */
int is_palindrome(char *s)
{
	int i;

	i = 0;

	if (*s == '\0')
		return (0);

	if (*s != '\0')
	{
/* 		i = _strlen_recursion(s + 1); */
	}
	return (1 + i);
}
