#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>

/**
 * main - a program prints the minimum number of coins to give in change.
 * @argc: argument count
 * @argv: argument vector containing two numbers.
 *
 * Return: 0 on success, 1 on error.
 */

int main(int argc, char **argv)
{
	(void)argc;
	(void)argv;


	return (0);
}
